<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.study.s1.member.MemberDAO">
    	<insert id="join" parameterType="MemberDTO">
    	INSERT INTO MEMBER VALUES (#{id},#{pw},#{name},#{email},#{phone})
    	</insert>
    	<select id="login" parameterType="MemberDTO" resultType="MemberDTO">
    	SELECT * FROM MEMBER WHERE ID=#{id} and pw=#{pw}
    	</select>
    	<select id="mypage" parameterType="MemberDTO" resultType="MemberDTO">
    	SELECT ID,NAME,EMAIL,PHONE FROM MEMBER WHERE ID=#{id} and PW=#{pw}
    	</select>
    </mapper>